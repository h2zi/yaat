# 参与 YAAT 开发

YAAT 会读取本机 AI 客户端配置、会话元数据和系统凭据槽。开发与验证必须把“不接触真实账号”和“不整份覆盖用户配置”当作硬约束，而不是普通测试建议。

## 开发环境

- Rust `1.97.1`，由 `rust-toolchain.toml` 固定；crate manifest 同时声明相同的最低支持版本。
- Node.js 22.x 与 pnpm 11.18.0（由 `package.json` 固定）。
- Docker（推荐用于完整构建和测试）。
- 仅打包对应桌面平台时才需要 Tauri 的系统依赖：macOS 使用 Xcode Command Line Tools，Windows 使用 Visual Studio C++ Build Tools 与 WebView2。

安装前端依赖：

```bash
pnpm install --frozen-lockfile
```

不要在包含真实 Codex、Claude Code 或 Claude Desktop 账号的日常开发用户下启动 YAAT，也不要让测试读取真实的 `~/.codex`、`~/.claude`、系统钥匙串或 Claude Desktop 用户目录。浏览器预览使用内置虚构数据，不连接 Tauri 后端：

```bash
VITE_YAAT_PREVIEW=1 pnpm run dev
```

`pnpm run tauri dev` 只适用于一次性测试用户、虚拟机或其他已经确认不含真实客户端数据的隔离环境。

## 代码质量门禁

宿主机可以运行不会启动应用、不会执行测试、不会读取用户账号数据的静态门禁：

```bash
cargo fmt --all -- --check
cargo check --workspace --all-targets --locked
cargo clippy --workspace --all-targets --locked -- -D warnings
pnpm run format:check
pnpm run typecheck
pnpm run build
```

完整门禁在 Docker 中执行 workspace check、Clippy、`-D warnings` Rust 文档、Rust 测试、前端构建和 Tauri 无安装包构建：

```bash
docker build --file docker/app-build.Dockerfile --tag yaat-app-build .
docker build --file docker/windows-check.Dockerfile --tag yaat-windows-check .
```

真实客户端互操作镜像还会检查各自独立 Cargo workspace 的格式、Clippy 与 locked release build；运行阶段使用固定客户端版本、虚构凭据、断网容器和移除后的 Linux capabilities：

```bash
sh scripts/test-codex-docker.sh
sh scripts/test-claude-docker.sh
```

Windows Docker 镜像交叉检查全部 Rust targets，包括 Windows FFI 与条件编译分支；DPAPI、Credential Manager 和真实系统钥匙串故障路径仍需在对应平台的隔离 CI 或测试用户中运行。交叉编译通过不等于完成真机门禁。

## CI 与发布

`.github/workflows/ci.yml` 在 `main` 推送和 Pull Request 上执行前端格式、类型与构建检查，并在 Linux、macOS、Windows 上执行 Rust check、Clippy 和测试。Linux 额外检查 rustfmt 与 rustdoc。

正式发布前，把同一个版本写入以下三处：

- `package.json` 的 `version`
- 根 `Cargo.toml` 的 `workspace.package.version`
- `src-tauri/tauri.conf.json` 的 `version`

Tauri updater 的私钥不得进入仓库。仓库只保存可公开的验证公钥；GitHub Actions 从仓库 Secret `TAURI_SIGNING_PRIVATE_KEY` 读取私钥。配置 Secret 后，在 Actions 页面手动运行 `Release` 工作流。工作流会创建 `v<version>` 标签和草稿 Release，上传 macOS 双架构与 Windows x64 安装包、更新签名及 `latest.json`。检查产物后手动发布草稿；应用不会读取草稿 Release。

本机验证正式打包时使用私钥路径，不要复制到项目目录：

```bash
TAURI_SIGNING_PRIVATE_KEY_PATH=/path/to/yaat-updater.key pnpm run tauri build
```

Updater 签名与 Apple Developer ID / Windows Authenticode 是三套独立签名。前者用于 YAAT 验证下载内容，后两者用于操作系统验证发行者；没有平台证书时仍能生成 updater 产物，但首次安装会显示操作系统的未签名警告。

## Rust 与 Tauri 约定

- `src-tauri/src/lib.rs` 是 Tauri 组合根；业务逻辑留在普通 Rust 模块中，IPC 命令只负责反序列化、验证、编排和稳定错误映射。
- 平台适配器只声明自己拥有的配置路径。所有用户配置写入都必须经过 `activation` 的语义白名单、compare-and-swap 原子发布和可验证回滚。
- 后端持久化所需的凭据结构可以实现 Serde，但不能派生会输出明文的 `Debug`，也不能进入普通账号列表或其他非显式 Tauri 响应。只有按账号 ID 调用的凭据读取命令可以回显完整值。
- `unsafe` 只用于缺少安全封装的系统 FFI；每个 block 都必须写明指针生命周期、编码、可空参数或其他安全前提。workspace lint 会拒绝无说明的 `unsafe`。
- 注释解释所有权、安全边界、格式兼容或失败语义，不复述代码。公开补丁 API 和共享 IPC 契约需要 rustdoc；显然的私有辅助函数不添加模板化说明。
- 不用 `unwrap()` / `expect()` 处理可由用户输入、文件、系统钥匙串或子进程触发的生产错误。测试可以使用它们表达 fixture 前提。
- `tauri-plugin-single-instance` 必须是 Builder 注册的第一个插件。

## IPC 与权限同步

新增、重命名或删除 Tauri 命令时，后端权限边界与前端调用清单必须同步检查四处：

1. `src-tauri/src/lib.rs` 中的 `tauri::generate_handler!`。
2. `src-tauri/build.rs` 中传给 `AppManifest` 的命令清单。
3. `src-tauri/capabilities/default.json` 中授予 `main` 窗口的权限。
4. `src/api.ts` 中限制 `invoke` 的类型化命令清单。

`src-tauri/permissions/autogenerated/` 和 `src-tauri/gen/schemas/` 由 Tauri 生成，不手工编辑。前端不得通过新增插件绕过现有命令边界；如果确实需要新权限，应先收紧 command scope，再更新 capability 和架构文档。

## 前端约定

- `src/types.ts` 必须与 `yaat-contracts` 的 serde 名称保持一致：对象字段使用 `camelCase`，枚举值使用 `snake_case`。
- 所有后端调用集中在 `src/api.ts`；组件不直接调用 `invoke`。
- `VITE_YAAT_PREVIEW=1` 只使用 `src/preview.ts` 中的虚构数据。生产构建不得包含真实路径、账号或密钥 fixture。
- 文案同时维护简体中文和英文，新增 key 后要检查两种语言均有值。
- 两个 `tools/*-interop` 是独立的二进制 Cargo workspace；更新其依赖时同时更新各自的 `Cargo.lock`，Docker 始终使用 `--locked` 构建。

## 提交前检查

- 配置补丁没有扩大适配器的 owned paths，也没有把字段级更新改成整份序列化覆盖。
- 错误路径仍然失败关闭，不会把客户端密文误作 YAAT 明文、静默忽略钥匙串错误或把外部编辑当成缺项。
- 新文件不会跟随扫描目录中的符号链接，受控文件仍有大小上限和 UTF-8 / 结构校验。
- 没有修改 Tauri 生成文件、图标、锁文件或固定互操作版本，除非该修改就是本次变更的明确目标。
- README、`docs/architecture.md` 和第三方声明与实际支持范围一致。
